server:
  port: 8085

spring:
  application:
    name: messaging-service
  datasource:
    url: jdbc:postgresql://localhost:5432/daaakiya_db
    username: daaakiya
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379
