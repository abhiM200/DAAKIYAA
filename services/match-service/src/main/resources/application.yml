server:
  port: 8086

spring:
  application:
    name: match-service
  datasource:
    url: jdbc:postgresql://localhost:5432/daaakiya_db
    username: daaakiya
    password: password
  jpa:
    hibernate:
      ddl-auto: update
