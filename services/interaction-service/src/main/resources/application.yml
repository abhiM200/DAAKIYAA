server:
  port: 8084

spring:
  application:
    name: interaction-service
  datasource:
    url: jdbc:postgresql://localhost:5432/daaakiya_db
    username: daaakiya
    password: password
  jpa:
    hibernate:
      ddl-auto: update
